apiVersion: v1
kind: Service
metadata:
  name: student-management
  namespace: devops
spec:
  selector:
    app: student-management
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8089
      nodePort: 30089
  type: NodePort